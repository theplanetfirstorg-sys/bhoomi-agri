{
  "name": "bhoomi-agri",
  "version": "1.0.0",
  "description": "Bhoomi.Agri — Agricultural Intelligence Platform for Sri Lanka",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "migrate": "npm run migrate --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
